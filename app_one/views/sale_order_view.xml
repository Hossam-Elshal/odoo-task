<odoo>
    <data>
        <record id="view_order_form_discount_approval" model="ir.ui.view">
            <field name="name">sale.order.form.discount.approval</field>
            <field name="model">sale.order</field>
            <field name="inherit_id" ref="sale.view_order_form"/>
            <field name="arch" type="xml">

                <xpath expr="//header" position="inside">
                    <field name="approval_state" widget="statusbar" statusbar_visible="not_required,pending,approved"/>
                    <field name="has_high_discount" invisible="1"/>
                </xpath>

                <!-- Approve Button -->
                <xpath expr="//header" position="inside">
                    <button name="action_approve_discount"
                            type="object"
                            string="Approve Discount"
                            class="btn btn-warning"
                            invisible="approval_state != 'pending'"
                            groups="app_one.sale_manager_group"/>
                </xpath>

                <!-- Warning Alert -->
                <xpath expr="//sheet" position="before">
                    <div class="alert alert-warning" role="alert"
                         invisible="approval_state != 'pending'">
                        <strong>⚠️ Manager Approval Required!</strong>
                        <br/>
                        This order has a discount greater than 10% and requires manager approval.
                    </div>
                </xpath>

            </field>
        </record>
    </data>
</odoo>